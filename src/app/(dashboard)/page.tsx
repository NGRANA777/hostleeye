import DashboardClient from "@/components/DashboardClient";

export const metadata = {
  title: "Dashboard | Hostel Security",
};

export default function DashboardPage() {
  return <DashboardClient />;
}
